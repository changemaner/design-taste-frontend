# Freeplay Declaration — 自由发挥模式契约表单（自创风格）

> 本表单是 **自由发挥模式**（brief 无法匹配素材库时）的 mini design-system 规格。
> 生成 deck **之前**必须填好本表；它是"生成纪律"和"校验锚点"的同一份来源。
> 对照素材模式的 DESIGN.md：二者**同构**（同节骨架），区别只在契约来源——
> 素材模式 = 对齐外部 DESIGN.md（值层 1:1）；自由发挥 = 对齐本表自创声明（值层自洽）。
>
> 填写纪律：先填 ①②③，让声音成型；再填 ④⑤ 让声音落成 token；⑥⑦ 锁定边界与反趋同；⑧ 诚实收尾。

---

## 0. 问诊（先在 0.E 问清，填表前）
一次问完 3-4 问：**风格意象**（像什么？给出可对照的现实参照）/ **语气词**（3-5 个词描述气质）/ **明确禁忌**（不要什么）/ **受众**（给谁看）。
答案浓缩后写进 ① 的 description 里。

## ① description — 一句话描述整个系统的"声音"（必填）
> 一段话：载体 / 表面 / 主色 / 显示与正文字体角色 / 视觉隐喻 / 整体气质。要具体到"读起来像什么"，不是形容词堆砌。

## ② palette voice — 命名色板 + 语义职责（必填，≥3 色 + 每色一句职责）
| token | 色值 | 职责（在哪用，不在哪用） |
|---|---|---|
| `--c-bg` | `#...` | 画布/默认表面 |
| `--c-ink` | `#...` | 结构色：文字/边框/阴影 |
| `--c-accent` | `#...` | 唯一强调色：命中页面唯一 accent |
| … | | |

反趋同：整组色板**不得等于**库内任何一套素材的色板；拒绝 8 俗色（AI-purple `#6366f1`、默认渐变紫、纯白底+纯黑字等）。

## ③ typographic voice — 角色化字体 token（必填，含 CJK）
| token | 字体（Latin） | 字重 | clamp 字号 | 行高 | 字距 | 用途 |
|---|---|---|---|---|---|---|
| `disp-hero` | '…' | 900 | clamp(…px, …vw, …px) | | | 封面主标题 |
| `body` | '…' | 400 | clamp(…px, …vw, …px) | | | 正文 |
| `micro` | '…' | 700 | … | | +0.16em | 分类标签 |
| … | | | | | | |

> 尺寸/间距沿用 15.4 纪律（×2）；CJK 配对填第 ⑧ 节，不填在这。

## ④ signature moves — 自创签名动作（必填 2-4 个，每个一句规格 + 一句用途）
1. **动作名**：规格（尺寸/颜色/变换）—— 用途（哪类页面、承担什么）。
2. …
> 对应 16 章 signature moves；每条必须能追溯到本表某行，否则删掉（restraint is the move）。

## ⑤ spacing / 布局常量
| 项 | 值 |
|---|---|
| frame inset | clamp(…px, …vw, …px) |
| 底部安全区 | 复用 `--safety-bottom`（≥120px） |

## ⑥ 边界（预填常量，不开放 — 借 15 章 + Zara Fixed-Stage）
- 画布：1920×1080，整体缩放、禁重排（15.2）
- 切页：`visibility/opacity` 切换，禁 `display:none`
- 导航：底部居中自适应 pill dot nav（15.2）
- 动效：v5 editorial expo（15.5，transition 驱动）
- 布局安全：内容最低 y ≤ 900，安全区 ≥120px（15.4）
- CONFIG：`--motion-*` / `--nav-*` / `--safety-bottom` / `--frame-pad-*`（15.8）
- 页面计划：默认 8 页（15.1）

## ⑦ 反趋同差异化声明（必填一句，写不出 = 重设计）
> "本套与库内 30 套素材的差异点在于：**______**。"
> （将来可作 P1.8 配方回收的 tagline；必须具体到可验证，不许写"配色不同"这类空话。）

## ⑧ CJK 方案（内容含中文/混排时必填，否则可跳过）
| 角色 | Latin 字体 | 中文配对 | 字重匹配 |
|---|---|---|---|
| Display | … | 思源宋体/思源黑体 或 品牌适当字体 | 匹配字重预算 |
| Body | … | 思源黑体（Noto Sans SC） | 匹配 |
| Mono | … | 保留 Latin mono + CJK fallback | — |

- 行高：CJK body 1.75-1.85，display 1.15-1.25；字距归零；禁 uppercase/italic；盘古之白；`display=swap` 加载
- 已知缺口（诚实标注）：如"该中文显示字体只有单字重，无法匹配 900 的大声量"→ 写明补偿策略

## ⑨ 已知缺口（必填至少一条，诚实收尾）
> 本套自创的已知局限：**______**。（如字体许可、单字重、某布局脆弱点——不写"无"，没有完美的系统。）

---

## 校验锚点（P1.7-B 用它）
QA 时按本表逐项核对：deck 实际用色/字体 ⊆ 本表 ②③ 声明的 token 集；边界合规见 ⑥；CJK 见 ⑧。
本表即将来 P1.8 配方回收时 `styles/custom/<slug>/design.md` 的雏形。
